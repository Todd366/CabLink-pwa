const fs = require('fs');
const path = require('path');

function replaceOnce(content, oldStr, newStr, label) {
  const idx = content.indexOf(oldStr);
  if (idx === -1) {
    console.error('❌ Could not find anchor for: ' + label);
    return content;
  }
  return content.slice(0, idx) + newStr + content.slice(idx + oldStr.length);
}

fs.writeFileSync(path.join('backend','services','push_service.js'), "const webpush = require(\"web-push\");\nconst fs = require(\"fs\");\nconst path = require(\"path\");\n\nconst SUBSCRIPTIONS_FILE = path.join(__dirname, \"..\", \"data\", \"push_subscriptions.json\");\n\nconst VAPID_PUBLIC_KEY = process.env.VAPID_PUBLIC_KEY;\nconst VAPID_PRIVATE_KEY = process.env.VAPID_PRIVATE_KEY;\nconst VAPID_SUBJECT = process.env.VAPID_SUBJECT || \"mailto:admin@cablink.app\";\n\nlet configured = false;\n\nif (VAPID_PUBLIC_KEY && VAPID_PRIVATE_KEY) {\n    webpush.setVapidDetails(VAPID_SUBJECT, VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY);\n    configured = true;\n} else {\n    console.warn(\"\u26a0\ufe0f  Push notifications disabled \u2014 VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY not set in .env\");\n}\n\nfunction loadSubscriptions() {\n    if (!fs.existsSync(SUBSCRIPTIONS_FILE)) return { subscriptions: [] };\n    try {\n        const parsed = JSON.parse(fs.readFileSync(SUBSCRIPTIONS_FILE, \"utf8\"));\n        return { subscriptions: Array.isArray(parsed.subscriptions) ? parsed.subscriptions : [] };\n    } catch (error) {\n        return { subscriptions: [] };\n    }\n}\n\nfunction saveSubscriptions(data) {\n    fs.writeFileSync(SUBSCRIPTIONS_FILE, JSON.stringify(data, null, 2), \"utf8\");\n}\n\n// Save (or replace) a push subscription for a given account/device.\nfunction save({ accountId, role, subscription }) {\n    if (!subscription || !subscription.endpoint) {\n        throw new Error(\"Invalid push subscription\");\n    }\n\n    const data = loadSubscriptions();\n\n    // One active subscription per endpoint \u2014 replace if it already exists\n    // (e.g. re-subscribing after a permission reset).\n    data.subscriptions = data.subscriptions.filter(\n        s => s.subscription.endpoint !== subscription.endpoint\n    );\n\n    data.subscriptions.push({\n        accountId: accountId || null,\n        role: role || \"unknown\",\n        subscription,\n        savedAt: new Date().toISOString()\n    });\n\n    saveSubscriptions(data);\n}\n\nasync function sendToAccount(accountId, payload) {\n    if (!configured) return { sent: 0, skipped: \"not configured\" };\n\n    const data = loadSubscriptions();\n    const targets = data.subscriptions.filter(s => s.accountId === accountId);\n\n    return sendToSubscriptions(targets, payload);\n}\n\nasync function sendToOnlineDrivers(payload) {\n    if (!configured) return { sent: 0, skipped: \"not configured\" };\n\n    const data = loadSubscriptions();\n    const targets = data.subscriptions.filter(s => s.role === \"driver\");\n\n    return sendToSubscriptions(targets, payload);\n}\n\nasync function sendToSubscriptions(targets, payload) {\n    let sent = 0;\n    const stale = [];\n\n    for (const target of targets) {\n        try {\n            await webpush.sendNotification(target.subscription, JSON.stringify(payload));\n            sent += 1;\n        } catch (error) {\n            // 410/404 means the subscription is dead (uninstalled, expired) \u2014 clean it up.\n            if (error.statusCode === 410 || error.statusCode === 404) {\n                stale.push(target.subscription.endpoint);\n            }\n        }\n    }\n\n    if (stale.length) {\n        const data = loadSubscriptions();\n        data.subscriptions = data.subscriptions.filter(\n            s => !stale.includes(s.subscription.endpoint)\n        );\n        saveSubscriptions(data);\n    }\n\n    return { sent, staleRemoved: stale.length };\n}\n\nmodule.exports = {\n    save,\n    sendToAccount,\n    sendToOnlineDrivers,\n    isConfigured: () => configured,\n    publicKey: VAPID_PUBLIC_KEY\n};\n", 'utf8');
console.log('✅ backend/services/push_service.js created');

fs.writeFileSync(path.join('backend','routes','push_api.js'), "const router = require(\"express\").Router();\nconst push = require(\"../services/push_service\");\n\n// GET /api/push/vapid-public-key\n// Frontend needs this to call pushManager.subscribe()\nrouter.get(\"/push/vapid-public-key\", (req, res) => {\n    if (!push.isConfigured()) {\n        return res.status(503).json({ success: false, error: \"Push notifications not configured on this server\" });\n    }\n    res.json({ success: true, publicKey: push.publicKey });\n});\n\n// POST /api/push/subscribe\n// body: { accountId, role: 'driver'|'passenger', subscription: <PushSubscription> }\nrouter.post(\"/push/subscribe\", (req, res) => {\n    try {\n        push.save(req.body || {});\n        res.json({ success: true });\n    } catch (error) {\n        res.status(400).json({ success: false, error: error.message });\n    }\n});\n\nmodule.exports = router;\n", 'utf8');
console.log('✅ backend/routes/push_api.js created');

fs.writeFileSync(path.join('sw.js'), "// CabLink Service Worker v2\nconst CACHE_NAME = 'cablink-v2';\nconst STATIC_ASSETS = [\n  '/',\n  '/index.html',\n  '/manifest.json'\n];\n\nself.addEventListener('install', function(e) {\n  e.waitUntil(\n    caches.open(CACHE_NAME).then(function(cache) {\n      return cache.addAll(STATIC_ASSETS).catch(function() {\n        // Non-fatal \u2014 cache what we can\n      });\n    })\n  );\n  self.skipWaiting();\n});\n\nself.addEventListener('activate', function(e) {\n  e.waitUntil(\n    caches.keys().then(function(keys) {\n      return Promise.all(\n        keys.filter(function(k) { return k !== CACHE_NAME; })\n            .map(function(k) { return caches.delete(k); })\n      );\n    })\n  );\n  self.clients.claim();\n});\n\nself.addEventListener('fetch', function(e) {\n  // Network first \u2014 fall back to cache\n  if (e.request.url.includes('/api/')) return; // never cache API calls\n  e.respondWith(\n    fetch(e.request).catch(function() {\n      return caches.match(e.request);\n    })\n  );\n});\n\n// ============================================================\n// PUSH NOTIFICATIONS\n// ============================================================\n\nself.addEventListener('push', function(e) {\n  let data = {};\n  try { data = e.data ? e.data.json() : {}; } catch (err) {}\n\n  const title = data.title || 'CabLink';\n  const options = {\n    body: data.body || '',\n    tag: data.tag || 'cablink',\n    data: data.data || {},\n    icon: '/icons/icon-192.png',\n    badge: '/icons/icon-192.png'\n  };\n\n  e.waitUntil(self.registration.showNotification(title, options));\n});\n\nself.addEventListener('notificationclick', function(e) {\n  e.notification.close();\n  e.waitUntil(\n    self.clients.matchAll({ type: 'window' }).then(function(clients) {\n      for (const client of clients) {\n        if ('focus' in client) return client.focus();\n      }\n      if (self.clients.openWindow) return self.clients.openWindow('/');\n    })\n  );\n});\n", 'utf8');
console.log('✅ sw.js rewritten with push support');

const appPath = path.join('backend','server','app.js');
let appJs = fs.readFileSync(appPath, 'utf8');
appJs = replaceOnce(appJs, "const leaderboardRoutes =\n    require(\"../routes/leaderboard_api\");", "const leaderboardRoutes =\n    require(\"../routes/leaderboard_api\");\n\nconst pushRoutes =\n    require(\"../routes/push_api\");", 'app.js require push');
appJs = replaceOnce(appJs, "app.use(\n    \"/api\",\n    leaderboardRoutes\n);", "app.use(\n    \"/api\",\n    leaderboardRoutes\n);\n\napp.use(\n    \"/api\",\n    pushRoutes\n);", 'app.js mount push');
fs.writeFileSync(appPath, appJs, 'utf8');
console.log('✅ backend/server/app.js patched');

const pkgPath = path.join('package.json');
let pkg = fs.readFileSync(pkgPath, 'utf8');
pkg = replaceOnce(pkg, "    \"leaflet\": \"^1.9.4\",\n    \"react\": \"^19.1.1\",\n    \"react-dom\": \"^19.1.1\"\n  },", "    \"leaflet\": \"^1.9.4\",\n    \"react\": \"^19.1.1\",\n    \"react-dom\": \"^19.1.1\",\n    \"web-push\": \"^3.6.7\"\n  },", 'package.json web-push dep');
fs.writeFileSync(pkgPath, pkg, 'utf8');
console.log('✅ package.json: added web-push dependency (run npm install after this)');

const enginePath = path.join('backend','canonical','ride_engine.js');
let engine = fs.readFileSync(enginePath, 'utf8');
engine = replaceOnce(engine, "const repository =\n    require(\"./ride_repository\");", "const repository =\n    require(\"./ride_repository\");\n\nconst push =\n    require(\"../services/push_service\");", 'ride_engine.js require push');
engine = replaceOnce(engine, "    return repository.create(\n        ride\n    );\n\n}", "return repository.create(\n        ride\n    ).then(createdRide => {\n\n        // Fire-and-forget: notify any subscribed online drivers that a\n        // ride is available. A push failure here should never block or\n        // fail the ride creation itself.\n        push.sendToOnlineDrivers({\n            title: \"\ud83d\ude95 New ride request\",\n            body: `${createdRide.pickup} \u2192 ${createdRide.dropoff} \u00b7 P${createdRide.fare}`,\n            tag: \"cablink-ride-\" + createdRide.id,\n            data: { rideId: createdRide.id, type: \"NEW_RIDE\" }\n        }).catch(() => {});\n\n        return createdRide;\n\n    });\n\n}", 'ride_engine.js notify drivers on create');
engine = replaceOnce(engine, "async function acceptRide(\n    id,\n    driverId,\n    driverName\n) {\n\n    return repository.accept(\n        id,\n        driverId,\n        driverName\n    );\n\n}", "async function acceptRide(\n    id,\n    driverId,\n    driverName\n) {\n\n    return repository.accept(\n        id,\n        driverId,\n        driverName\n    ).then(result => {\n\n        if (result && result.ride && result.ride.passenger) {\n\n            push.sendToAccount(result.ride.passenger, {\n                title: \"\ud83d\ude97 Driver assigned!\",\n                body: `${driverName || \"A driver\"} is on the way`,\n                tag: \"cablink-ride-\" + id,\n                data: { rideId: id, type: \"DRIVER_ASSIGNED\" }\n            }).catch(() => {});\n\n        }\n\n        return result;\n\n    });\n\n}\n\n", 'ride_engine.js notify passenger on accept');
fs.writeFileSync(enginePath, engine, 'utf8');
console.log('✅ backend/canonical/ride_engine.js patched');

const idxPath = path.join('frontend','index.html');
let html = fs.readFileSync(idxPath, 'utf8');
html = replaceOnce(html, "  window.addEventListener('online', () => { checkOnline(); syncOfflineQueue(); });\n  window.addEventListener('offline', checkOnline);\n});", "  window.addEventListener('online', () => { checkOnline(); syncOfflineQueue(); });\n  window.addEventListener('offline', checkOnline);\n  registerServiceWorkerAndPush();\n});\n\nasync function registerServiceWorkerAndPush(){\n  if(!('serviceWorker' in navigator)) return;\n  try{\n    await navigator.serviceWorker.register('/sw.js');\n  }catch(e){\n    console.log('Service worker registration failed', e);\n  }\n}\n\n// Call this after a driver goes online, or a passenger books a ride,\n// so they can actually receive a push when something happens even if\n// the app isn't open. Silently does nothing if the browser or user\n// doesn't support/allow notifications \u2014 this should never block the\n// core booking/driving flow.\nasync function subscribeToPush(role){\n  if(!('serviceWorker' in navigator) || !('PushManager' in window)) return;\n\n  try{\n    const permission = await Notification.requestPermission();\n    if(permission !== 'granted') return;\n\n    const reg = await navigator.serviceWorker.ready;\n\n    const keyRes = await fetch('/api/push/vapid-public-key');\n    const keyData = await keyRes.json();\n    if(!keyData.success) return; // push not configured on this server yet\n\n    let subscription = await reg.pushManager.getSubscription();\n    if(!subscription){\n      subscription = await reg.pushManager.subscribe({\n        userVisibleOnly: true,\n        applicationServerKey: urlBase64ToUint8Array(keyData.publicKey)\n      });\n    }\n\n    const clAccountRaw = localStorage.getItem('cl_account');\n    const clAccount = clAccountRaw ? JSON.parse(clAccountRaw) : null;\n\n    await fetch('/api/push/subscribe', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({\n        accountId: (clAccount && clAccount.id) || null,\n        role,\n        subscription\n      })\n    });\n  }catch(e){\n    console.log('Push subscription skipped:', e.message);\n  }\n}\n\nfunction urlBase64ToUint8Array(base64String){\n  const padding = '='.repeat((4 - base64String.length % 4) % 4);\n  const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');\n  const rawData = atob(base64);\n  const outputArray = new Uint8Array(rawData.length);\n  for(let i = 0; i < rawData.length; i++){\n    outputArray[i] = rawData.charCodeAt(i);\n  }\n  return outputArray;\n}", 'index.html sw registration + subscribe fns');
html = replaceOnce(html, "if(!clAccount){\ntoast('Log in on the Profile tab so your rides and THB track to your real account', 'warning');\n}\n\n\nlocalStorage.setItem(", "if(!clAccount){\ntoast('Log in on the Profile tab so your rides and THB track to your real account', 'warning');\n}\n\nsubscribeToPush('driver');\n\n\nlocalStorage.setItem(", 'index.html driver subscribe hook');
html = replaceOnce(html, "  payload.passenger = payload.passengerId; // canonical backend field name", "  payload.passenger = payload.passengerId; // canonical backend field name\n  subscribeToPush('passenger');", 'index.html passenger subscribe hook');
fs.writeFileSync(idxPath, html, 'utf8');
console.log('✅ frontend/index.html patched');

console.log('');
console.log('Done. Next steps:');
console.log('1. Add to your .env: VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT');
console.log('2. npm install   (pulls in the new web-push package)');
console.log('3. Restart backend: Ctrl+C then npm run backend');
