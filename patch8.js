const fs = require('fs');
const path = require('path');

function replaceOnce(content, oldStr, newStr, label) {
  const idx = content.indexOf(oldStr);
  if (idx === -1) {
    console.error('❌ Could not find anchor for: ' + label);
    return content;
  }
  return content.slice(0, idx) + newStr + content.slice(idx + oldStr.length);
}

fs.writeFileSync(path.join('backend','routes','marketplace_api.js'), "const router = require(\"express\").Router();\nconst rideEngine = require(\"../canonical/ride_engine\");\n\n// ============================================================\n// DIGITAL MALL (bstm-marketplace-app) INTEGRATION\n//\n// This lets any other BSTM Ecosystem property \u2014 starting with\n// the Digital Mall marketplace \u2014 hand a delivery task to CabLink\n// and have it dispatched through the exact same real driver\n// network as a normal passenger ride (same map, same accept\n// flow, same THB reward pipeline). It is NOT a separate system \u2014\n// a marketplace task IS a canonical ride, just tagged with\n// source: \"digital_mall\" and taskType: \"delivery\" so the driver\n// app and admin panel can show it differently.\n//\n// AUTH: server-to-server only. Requires a shared secret in the\n// x-marketplace-key header, set via MARKETPLACE_API_KEY in .env.\n// This is NOT meant to be called from a browser \u2014 it's meant to\n// be called from the Digital Mall's own backend.\n// ============================================================\n\nconst MARKETPLACE_API_KEY = process.env.MARKETPLACE_API_KEY || \"\";\n\nfunction requireMarketplaceAuth(req, res, next) {\n    if (!MARKETPLACE_API_KEY) {\n        return res.status(503).json({\n            success: false,\n            error: \"Marketplace integration not configured \u2014 set MARKETPLACE_API_KEY in .env\"\n        });\n    }\n\n    if (req.headers[\"x-marketplace-key\"] !== MARKETPLACE_API_KEY) {\n        return res.status(401).json({ success: false, error: \"Invalid marketplace API key\" });\n    }\n\n    next();\n}\n\n// POST /api/marketplace/tasks\n//\n// body: {\n//   orderId: string          \u2014 Digital Mall's own order/reference ID (required)\n//   pickup: string            \u2014 where the driver collects the item (required)\n//   dropoff: string           \u2014 where it's delivered (required)\n//   itemDescription: string   \u2014 what's being delivered\n//   customerName: string\n//   customerPhone: string\n//   fare: number               \u2014 BWP amount, in whole units\n//   notes: string\n// }\n//\n// Returns the created ride, including its id, which the\n// marketplace should store to poll status later.\nrouter.post(\"/marketplace/tasks\", requireMarketplaceAuth, async (req, res) => {\n    try {\n        const {\n            orderId,\n            pickup,\n            dropoff,\n            itemDescription,\n            customerName,\n            customerPhone,\n            fare,\n            notes\n        } = req.body || {};\n\n        if (!orderId || !pickup || !dropoff) {\n            return res.status(400).json({\n                success: false,\n                error: \"orderId, pickup, and dropoff are required\"\n            });\n        }\n\n        const ride = await rideEngine.createRide({\n            pickup,\n            dropoff,\n            fare: fare || 25,\n            notes: [itemDescription, notes].filter(Boolean).join(\" \u2014 \"),\n            source: \"digital_mall\",\n            taskType: \"delivery\",\n            externalRef: orderId,\n            passenger: customerName || \"Digital Mall customer\",\n            passengerName: customerName || null\n        });\n\n        res.json({\n            success: true,\n            ride,\n            statusUrl: `/api/marketplace/tasks/${ride.id}/status`\n        });\n\n    } catch (error) {\n        res.status(500).json({ success: false, error: error.message });\n    }\n});\n\n// GET /api/marketplace/tasks/:id/status\nrouter.get(\"/marketplace/tasks/:id/status\", requireMarketplaceAuth, async (req, res) => {\n    try {\n        const ride = await rideEngine.getRide(req.params.id);\n\n        if (!ride) {\n            return res.status(404).json({ success: false, error: \"Task not found\" });\n        }\n\n        res.json({\n            success: true,\n            taskId: ride.id,\n            externalRef: ride.externalRef,\n            status: ride.status,\n            driverName: ride.driverName || null,\n            updatedAt: ride.updatedAt\n        });\n\n    } catch (error) {\n        res.status(500).json({ success: false, error: error.message });\n    }\n});\n\nmodule.exports = router;\n", 'utf8');
console.log('✅ backend/routes/marketplace_api.js created — Digital Mall integration endpoint');

const enginePath = path.join('backend','canonical','ride_engine.js');
let engine = fs.readFileSync(enginePath, 'utf8');
engine = replaceOnce(engine, "        notes:\n            data.notes ||\n            \"\",\n\n        passenger:", "        notes:\n            data.notes ||\n            \"\",\n\n        source:\n            data.source ||\n            \"app\",\n\n        taskType:\n            data.taskType ||\n            \"passenger\",\n\n        externalRef:\n            data.externalRef ||\n            null,\n\n        passenger:", 'ride_engine.js source/taskType fields');
fs.writeFileSync(enginePath, engine, 'utf8');
console.log('✅ backend/canonical/ride_engine.js patched');

const appPath = path.join('backend','server','app.js');
let appJs = fs.readFileSync(appPath, 'utf8');
appJs = replaceOnce(appJs, "const pushRoutes =\n    require(\"../routes/push_api\");", "const pushRoutes =\n    require(\"../routes/push_api\");\n\nconst marketplaceRoutes =\n    require(\"../routes/marketplace_api\");", 'app.js require marketplace');
appJs = replaceOnce(appJs, "app.use(\n    \"/api\",\n    pushRoutes\n);", "app.use(\n    \"/api\",\n    pushRoutes\n);\n\napp.use(\n    \"/api\",\n    marketplaceRoutes\n);", 'app.js mount marketplace');
fs.writeFileSync(appPath, appJs, 'utf8');
console.log('✅ backend/server/app.js patched');

const idxPath = path.join('frontend','index.html');
let html = fs.readFileSync(idxPath, 'utf8');
html = replaceOnce(html, ".btn-primary{background:linear-gradient(135deg,var(--gold-l),var(--gold));color:#241a04;box-shadow:0 6px 20px -6px rgba(240,185,58,.5)}", ".btn-primary{background:linear-gradient(135deg,var(--gold-l),var(--gold));color:#241a04;box-shadow:0 3px 10px -4px rgba(240,185,58,.35)}", 'tone down primary button glow');
html = replaceOnce(html, ".nav-btn.center-btn{background:linear-gradient(135deg,var(--gold-l),var(--gold));color:#241a04;border-radius:50%;width:52px;height:52px;margin-top:-24px;padding:0;flex:none;align-self:flex-start;font-size:21px;box-shadow:0 6px 22px -4px rgba(240,185,58,.6);font-weight:700;min-width:52px}", ".nav-btn.center-btn{background:var(--surface3);color:var(--gold-l);border:1px solid var(--hair2);border-radius:50%;width:50px;height:50px;margin-top:-18px;padding:0;flex:none;align-self:flex-start;font-size:19px;box-shadow:0 4px 12px -4px rgba(0,0,0,.5);font-weight:700;min-width:50px}", 'redesign center nav button (less game-like)');
html = replaceOnce(html, "#surgeBanner{display:none;position:relative;z-index:2;background:linear-gradient(90deg,#7a4d0a,var(--gold));color:#241a04;padding:9px 18px;text-align:center;font-weight:700;font-size:12px;cursor:pointer;flex-shrink:0}", "#surgeBanner{display:none;position:relative;z-index:2;background:var(--surface2);border-bottom:1px solid rgba(240,185,58,.3);color:var(--gold-l);padding:9px 18px;text-align:center;font-weight:600;font-size:12px;cursor:pointer;flex-shrink:0;letter-spacing:.02em}", 'tone down surge banner');
html = replaceOnce(html, "function renderIncomingRequest(r){\n  STATE.driverRequests++;\n  const container = document.getElementById('driverRequests');\n  const div = document.createElement('div');\n  div.className = 'request-card pending'; div.id = 'req-' + r.id;\n  div.innerHTML = `<div style=\"display:flex;justify-content:space-between;margin-bottom:8px\"><strong>${r.id}</strong><span style=\"color:var(--gold-l);font-weight:700\">${r.fare||''} BWP</span></div>\n    <div style=\"font-size:12px;color:var(--muted);margin-bottom:8px\">\ud83d\udccd ${r.pickup||''} \u2192 \ud83c\udfc1 ${r.dropoff||''}</div>\n    <div style=\"display:flex;gap:8px\">\n      <button class=\"btn btn-green btn-sm\" onclick=\"window.acceptRideRequest('${r.id}',${r.fare||20})\">\u2713 Accept</button>\n      <button class=\"btn btn-outline btn-sm\" onclick=\"document.getElementById('req-${r.id}').remove()\">\u2717 Pass</button>\n    </div>`;\n  if(container.firstChild?.textContent?.includes('online')) container.innerHTML = '';\n  container.prepend(div);\n  updateDriverUI();\n  toast('\ud83d\ude97 New ride request received', 'warning'); haptic();\n}", "function renderIncomingRequest(r){\n  STATE.driverRequests++;\n  const container = document.getElementById('driverRequests');\n  const div = document.createElement('div');\n  div.className = 'request-card pending'; div.id = 'req-' + r.id;\n  const isMarketplace = r.source === 'digital_mall';\n  const badge = isMarketplace\n    ? `<span style=\"font-size:10px;font-weight:700;letter-spacing:.03em;color:var(--sapphire-l);border:1px solid var(--sapphire);border-radius:6px;padding:2px 6px;margin-right:6px\">DIGITAL MALL DELIVERY</span>`\n    : '';\n  div.innerHTML = `<div style=\"display:flex;justify-content:space-between;margin-bottom:8px\"><span>${badge}<strong>${r.id}</strong></span><span style=\"color:var(--gold-l);font-weight:700\">${r.fare||''} BWP</span></div>\n    <div style=\"font-size:12px;color:var(--muted);margin-bottom:8px\">\ud83d\udccd ${r.pickup||''} \u2192 \ud83c\udfc1 ${r.dropoff||''}</div>\n    ${r.notes ? `<div style=\"font-size:11px;color:var(--muted2);margin-bottom:8px\">${r.notes}</div>` : ''}\n    <div style=\"display:flex;gap:8px\">\n      <button class=\"btn btn-green btn-sm\" onclick=\"window.acceptRideRequest('${r.id}',${r.fare||20})\">\u2713 Accept</button>\n      <button class=\"btn btn-outline btn-sm\" onclick=\"document.getElementById('req-${r.id}').remove()\">\u2717 Pass</button>\n    </div>`;\n  if(container.firstChild?.textContent?.includes('online')) container.innerHTML = '';\n  container.prepend(div);\n  updateDriverUI();\n  toast(isMarketplace ? '\ud83d\udce6 New Digital Mall delivery received' : '\ud83d\ude97 New ride request received', 'warning'); haptic();\n}\n\n\n", 'Digital Mall delivery badge on request cards');
fs.writeFileSync(idxPath, html, 'utf8');
console.log('✅ frontend/index.html: visual refinements + Digital Mall badge applied');

console.log('');
console.log('Done. Next steps:');
console.log('1. Add to your .env: MARKETPLACE_API_KEY=a69baa18f15e4d6aa4f27a8945f79eefc7e285a3803bb24d');
console.log('2. Restart backend: Ctrl+C then npm run backend');
