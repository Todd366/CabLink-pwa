const fs = require('fs');
const path = require('path');

function replaceOnce(content, oldStr, newStr, label) {
  const idx = content.indexOf(oldStr);
  if (idx === -1) {
    console.error('❌ Could not find anchor for: ' + label);
    return content;
  }
  return content.slice(0, idx) + newStr + content.slice(idx + oldStr.length);
}

const idxPath = path.join('frontend', 'index.html');
let html = fs.readFileSync(idxPath, 'utf8');
html = replaceOnce(html, "<div class=\"screen\" id=\"s-driver\">\n  <div class=\"eyebrow\">Driver mode</div>\n  <div class=\"greeting\">Your shift</div>\n  <div style=\"display:flex;gap:8px;margin-bottom:12px\">\n    <button class=\"btn btn-outline btn-sm\" id=\"driverModeBtn\" onclick=\"toggleDriverMode()\" style=\"flex:1\">\ud83d\udfe2 Go online</button>\n    <button class=\"btn btn-outline btn-sm\" onclick=\"openModal('earningsModal')\" style=\"flex:1\">\ud83d\udcb0 Earnings</button>\n  </div>", "<div class=\"screen\" id=\"s-driver\">\n  <div id=\"s-driver-header\">\n  <div class=\"eyebrow\">Driver mode</div>\n  <div class=\"greeting\">Your shift</div>\n  <div style=\"display:flex;gap:8px;margin-bottom:12px\">\n    <button class=\"btn btn-outline btn-sm\" id=\"driverModeBtn\" onclick=\"toggleDriverMode()\" style=\"flex:1\">\ud83d\udfe2 Go online</button>\n    <button class=\"btn btn-outline btn-sm\" onclick=\"openModal('earningsModal')\" style=\"flex:1\">\ud83d\udcb0 Earnings</button>\n  </div>\n  </div>\n  ", 'wrap driver header for gating');
html = replaceOnce(html, "<div class=\"card-title\" style=\"margin-bottom:10px\">\ud83d\udccb Incoming requests</div>", "<div class=\"card-title\" id=\"driverRequestsTitle\" style=\"margin-bottom:10px\">\ud83d\udccb Incoming requests</div>", 'add id to Incoming requests title');
html = replaceOnce(html, "  function gateDriverScreen(){\n    var ds = document.getElementById('s-driver');\n    if(!ds) return;\n    var cta = document.getElementById('cl-drv-cta');\n    var walletBar = ds.querySelector('.wallet-bar');\n    var reqList = document.getElementById('driverRequests');\n    var goOnlineBtn = document.getElementById('driverModeBtn');\n\n    if(getRole() !== 'driver'){\n      if(walletBar) walletBar.style.display = 'none';\n      if(reqList) reqList.style.display = 'none';\n      if(goOnlineBtn) goOnlineBtn.style.display = 'none';\n      if(!cta){\n        cta = document.createElement('div');\n        cta.id = 'cl-drv-cta';\n        cta.className = 'card';\n        cta.style.textAlign = 'center';\n        cta.innerHTML = '<div style=\"font-size:44px;margin-bottom:10px\">\ud83d\ude96</div>'\n          + '<div style=\"font-weight:700;margin-bottom:8px\">Want to earn as a driver?</div>'\n          + '<div style=\"font-size:12px;color:var(--muted);margin-bottom:16px\">Apply once \u2014 after approval you can go online and receive real ride requests, earning 1 THB per trip.</div>'\n          + '<button class=\"btn btn-primary\" onclick=\"window.showDriverRegistrationForm()\">Apply to drive</button>';\n        ds.insertBefore(cta, ds.firstChild);\n      }\n    } else {\n      if(cta) cta.remove();\n      if(walletBar) walletBar.style.display = '';\n      if(reqList) reqList.style.display = '';\n      if(goOnlineBtn) goOnlineBtn.style.display = '';\n    }\n  }\n\n", "  function gateDriverScreen(){\n    var ds = document.getElementById('s-driver');\n    if(!ds) return;\n    var cta = document.getElementById('cl-drv-cta');\n    var header = document.getElementById('s-driver-header');\n    var walletBar = ds.querySelector('.wallet-bar');\n    var reqList = document.getElementById('driverRequests');\n    var reqTitle = document.getElementById('driverRequestsTitle');\n    var goOnlineBtn = document.getElementById('driverModeBtn');\n\n    if(getRole() !== 'driver'){\n      if(header) header.style.display = 'none';\n      if(walletBar) walletBar.style.display = 'none';\n      if(reqList) reqList.style.display = 'none';\n      if(reqTitle) reqTitle.style.display = 'none';\n      if(goOnlineBtn) goOnlineBtn.style.display = 'none';\n      if(!cta){\n        cta = document.createElement('div');\n        cta.id = 'cl-drv-cta';\n        cta.className = 'card';\n        cta.style.textAlign = 'center';\n        cta.innerHTML = '<div style=\"font-size:44px;margin-bottom:10px\">\ud83d\ude96</div>'\n          + '<div style=\"font-weight:700;margin-bottom:8px\">Want to earn as a driver?</div>'\n          + '<div style=\"font-size:12px;color:var(--muted);margin-bottom:16px\">Apply once \u2014 after approval you can go online and receive real ride requests, earning 1 THB per trip.</div>'\n          + '<button class=\"btn btn-primary\" onclick=\"window.showDriverRegistrationForm()\">Apply to drive</button>';\n        ds.insertBefore(cta, ds.firstChild);\n      }\n    } else {\n      if(cta) cta.remove();\n      if(header) header.style.display = '';\n      if(walletBar) walletBar.style.display = '';\n      if(reqList) reqList.style.display = '';\n      if(reqTitle) reqTitle.style.display = '';\n      if(goOnlineBtn) goOnlineBtn.style.display = '';\n    }\n  }\n\n", 'gateDriverScreen hides header+title too');
fs.writeFileSync(idxPath, html, 'utf8');
console.log('✅ frontend/index.html: driver screen gating fixed (no more Apply CTA + shift UI both showing)');

const lbPath = path.join('backend', 'routes', 'leaderboard_api.js');
let lb = fs.readFileSync(lbPath, 'utf8');
lb = replaceOnce(lb, "    const leaderboard = Object.values(byDriver)\n        .map(entry => {\n            const account = (accountsData.accounts || [])\n                .find(a => String(a.id) === String(entry.driverId));\n\n            return {\n                name: account\n                    ? account.name\n                    : (entry.driverId.length > 10\n                        ? entry.driverId.slice(0, 8) + \"\u2026\"\n                        : entry.driverId),\n                driverId: entry.driverId,\n                rides: entry.rides,\n                thb: Math.round(entry.thb * 10) / 10\n            };\n        })\n        .sort((a, b) => b.thb - a.thb)\n        .slice(0, 20);\n\n", "    const leaderboard = Object.values(byDriver)\n        .map(entry => {\n            const account = (accountsData.accounts || [])\n                .find(a => String(a.id) === String(entry.driverId));\n\n            return {\n                name: account ? account.name : null,\n                driverId: entry.driverId,\n                rides: entry.rides,\n                thb: Math.round(entry.thb * 10) / 10,\n                isRealAccount: !!account\n            };\n        })\n        // Only show drivers with a real, registered account \u2014 earlier\n        // test data left raw generated IDs (e.g. \"o840_DRIVER...\") with\n        // no account behind them, which looked like fake/junk entries.\n        .filter(entry => entry.isRealAccount)\n        .sort((a, b) => b.thb - a.thb)\n        .slice(0, 20);\n\n", 'leaderboard real-accounts-only filter');
fs.writeFileSync(lbPath, lb, 'utf8');
console.log('✅ backend/routes/leaderboard_api.js: stale test-data entries now hidden');

console.log('');
console.log('Done. Restart backend: Ctrl+C then npm run backend');

